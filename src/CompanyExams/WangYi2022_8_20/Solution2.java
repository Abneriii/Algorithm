package CompanyExams.WangYi2022_8_20;

/**
 * Author:
 * Created at:2022/8/20
 * Updated at:
 *
 *
 * 嘤嘤的长城easy
 **/
public class Solution2 {

    /**
     *
     * for num[i]=nums[1] to nums[n-1]
     *     if num[i-1]<nums[i+1]
     *         if(nums[i+1]!=nums[i]){
     *            把左边数字变为右边大小的数字
     *         }
     *         else{
     *             把右边数字变为左边大小
     *         }
     *     else
     *        if(nums[i-1]!=nums[i]){
     *            把右边的数字变为左边大小的数字
     *        }
     *        else{
     *            把左边数字变为右边大小的数字
     *        }
     *
     *
     *
     *
     *      * for num[i]=nums[1] to nums[n-1]
     *      *     if num[i-1]<nums[i+1]
     *      *         if(nums[i+1]!=nums[i]){
     *      *            把左边数字变为右边大小的数字
     *      *         }
     *      *         else{
     *      *             把右边数字变为左边大小
     *      *         }
     *      *     else
     *      *        if(nums[i-1]!=nums[i]){
     *      *            把右边的数字变为左边大小的数字
     *      *        }
     *      *        else{
     *      *            把左边数字变为右边大小的数字
     *      *        }
     *
     *
     * 把数组编程"长城"，对于长城中的每一个元素左右两边的元素相等，并且与它不相等。
     *
     *
     *
     *
     *
     *
     *
     *
     *
     *
     *
     */

}
